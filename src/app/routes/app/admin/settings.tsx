import { SettingsList } from '@/features/settings/component/settings-list';
import { Card } from 'antd';

export const SettingsRoute = () => {
  return (
    <Card>
      <SettingsList />
    </Card>
  );
};
